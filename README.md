# For first run (Chay lan dau)
npm install
npm start
# Init project

# 1. For first run :  
git submodule update --init
npm install
npm start

# 2. Update module : 
git submodule update --remote





